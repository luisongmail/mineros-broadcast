{
  "name": "@playflow/device-adapters",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Adaptadores de dispositivos de medición para béisbol — PlayFlow",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "*",
    "vitest": "^1.0.0"
  }
}
